#include <stdio.h>

/**
 * 怎么样在C层构建对象，并返回C层
 * @return
 */
int main() {
/*
    // 创建的class name全类名
    jclass j_class = env->FindClass("com/iamzzj/ffmpeg/MainActivity");

    构建java层对象，<init>
    jmethodID j_mid = env->GetMethodID(j_class, "<init>", "(II)V");

    // 构造方法，返回对象
    env->NewObject(j_class, j_mid, 1, 2);*/

    return 0;
}
